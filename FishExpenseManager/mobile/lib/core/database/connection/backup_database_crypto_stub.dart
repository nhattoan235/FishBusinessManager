Future<void> encryptPlaintextDatabase({
  required String sourcePath,
  required String destinationPath,
  required String encryptionKey,
}) =>
    throw UnsupportedError(
        'Không hỗ trợ chuyển đổi database trên nền tảng này.');
